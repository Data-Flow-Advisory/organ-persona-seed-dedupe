{
  "$comment": "VENDORED connector type vocabulary. The canonical source is the orchestrator's types.json (Data-Flow-Advisory/orchestrator @ feat/drift-gate). That repo/branch is not currently reachable (404), so this is a local copy of the standard JSON-primitive vocabulary used by the connection standard. When the orchestrator vocabulary becomes reachable, reconcile this file against it and drop any types this organ does not use. This organ uses only JSON-primitive types, so no proposed additions are needed.",
  "types": {
    "string": "A JSON string.",
    "integer": "A JSON integer.",
    "number": "A JSON number (integer or float).",
    "boolean": "A JSON boolean.",
    "object": "A JSON object (key/value map).",
    "array": "A JSON array."
  }
}
